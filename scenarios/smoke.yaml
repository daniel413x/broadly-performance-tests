execution:
  - scenario: smoke_test
    concurrency: 4
    ramp-up: 2m
    steps: 2
    hold-for: 2m

scenarios:
  smoke_test:
    script: "../test-plans/test-plan-1.jmx"

settings:
  env:
    TAG_SCENARIO: smoke

reporting:
- module: passfail
  criteria:
  - fail>2% for 1s, stop as failed
